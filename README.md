# Kevbot

A Discord bot built with Node.js and TypeScript.

## Setup Instructions
1. Clone the repository.
2. Install dependencies: `npm install`.
3. Create a `.env` file with your `DISCORD_TOKEN`.
4. Run the bot: `npx ts-node src/index.ts`.
